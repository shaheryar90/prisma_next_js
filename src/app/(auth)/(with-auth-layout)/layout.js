import React from "react";

export default function layoutDetail({ children }) {
  return (
    <>
      {children}
      <h2>On Login Page</h2>
    </>
  );
}

// in this we are making group routing
