import React from "react";

export default function PrivateRoute() {
  return <h2>you can't view this in the browser</h2>;
}

// _lib use for private folder with index.js and private folder itself. index.js will contain PrivateRoute component.
